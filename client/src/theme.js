import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#bf360c",
    },
    secondary: {
      main: "#00796b",
    },
  },
  background: {
    default: "#0288d1",
  },
});

export default theme;
