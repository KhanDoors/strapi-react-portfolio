import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#131A40",
    },
    secondary: {
      main: "#355B8C",
    },
  },
  background: {
    default: "#0288d1",
  },
});

export default theme;
